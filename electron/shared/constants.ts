/**
 * Shared constants used across main and renderer processes.
 */

/**
 * Default user ID for the single-user desktop application.
 * The schema supports multi-user but the app assumes a single local user.
 */
export const DEFAULT_USER_ID = 1;
